# Variables de ejemplo para cliente S3-compatible (MinIO). Copiar valores reales a .env (no commitear).
# SANOS_S3_ENDPOINT=http://minio:9000
# SANOS_S3_REGION=us-east-1
# SANOS_S3_BUCKET=sanos-media
# AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
# AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
# AWS_S3_FORCE_PATH_STYLE=true
